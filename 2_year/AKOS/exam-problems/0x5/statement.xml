<?xml version="1.0" encoding="utf-8" ?>
<problem
        package="c.ints"
        id="XXX"
        type="standard">
  <statement language="ru_RU">
    <description>

<p>На стандартном потоке ввода задается состояние таблицы
FAT файловой системы.</p>

<p>Первое число N задает число блоков файловой системы
(8 &lt;= N &lt;= 2<sup>20</sup>).
Затем следуют N - 4 записей о состоянии соответствующих блоков
Первые два блока не используются для размещения файлов,
то есть первый блок, в котором может размещаться файл,
имеет номер 2. Последние два блока также не используются
для размещения файлов.</p>

<p>Каждая запись о состоянии блока может быть равна 0,
что означает, что этот блок свободен, может содержать
номер следующего блока файловой системы,
может быть равна N - 2, что означает, что данный блок
нельзя использовать, или может быть равна N - 1,
что обозначает последний блок файла.</p>

<p>Затем на стандартном потоке ввода задаются номера
первого блока файла. Для каждого номера первого блока файла
на стандартный поток вывода напечатайте размер файла в блоках.</p>
      
</description>
  </statement>
  <examples>
    <example>
      <input>16
15 0 0 8 5 0 2 11 14 15 0 14
6
9</input>
<output>4
2</output>
    </example>
  </examples>
<review_comments>
</review_comments>
</problem>
